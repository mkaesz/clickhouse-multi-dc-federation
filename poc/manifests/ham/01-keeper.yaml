---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ham-keeper-config
  namespace: ham
data:
  keeper_config.xml: |
    <clickhouse>
      <keeper_server>
        <tcp_port>2181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse-keeper/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse-keeper/snapshots</snapshot_storage_path>
        <coordination_settings>
          <operation_timeout_ms>10000</operation_timeout_ms>
          <session_timeout_ms>30000</session_timeout_ms>
          <raft_logs_level>warning</raft_logs_level>
        </coordination_settings>
        <raft_configuration>
          <server>
            <id>1</id>
            <hostname>ham-keeper-0.ham-keeper.ham.svc.cluster.local</hostname>
            <port>9234</port>
          </server>
        </raft_configuration>
      </keeper_server>
      <logger>
        <level>warning</level>
        <console>1</console>
      </logger>
    </clickhouse>
---
apiVersion: v1
kind: Service
metadata:
  name: ham-keeper
  namespace: ham
  labels:
    app: ham-keeper
spec:
  clusterIP: None
  selector:
    app: ham-keeper
  ports:
    - name: client
      port: 2181
      targetPort: 2181
    - name: raft
      port: 9234
      targetPort: 9234
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ham-keeper
  namespace: ham
spec:
  serviceName: ham-keeper
  replicas: 1
  selector:
    matchLabels:
      app: ham-keeper
  template:
    metadata:
      labels:
        app: ham-keeper
    spec:
      containers:
        - name: keeper
          image: clickhouse/clickhouse-keeper:25.3
          ports:
            - containerPort: 2181
              name: client
            - containerPort: 9234
              name: raft
          volumeMounts:
            - name: config
              mountPath: /etc/clickhouse-keeper/keeper_config.xml
              subPath: keeper_config.xml
            - name: data
              mountPath: /var/lib/clickhouse-keeper
          readinessProbe:
            tcpSocket:
              port: 2181
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 2181
            initialDelaySeconds: 15
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: ham-keeper-config
        - name: data
          emptyDir: {}
